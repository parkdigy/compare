/********************************************************************************************************************
 * not null 확인 함수
 * @param v 확인할 값
 * @returns null 이 아니면 true, null 이면 false 반환
 * ******************************************************************************************************************/
export function isNotNull<T>(v: T): v is Exclude<T, null> {
  return v !== null;
}

export default isNotNull;
